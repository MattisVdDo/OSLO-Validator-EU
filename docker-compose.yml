version: "3.7"
services:
  frontend:
    build:
      context: ./packages/oslo-vue-frontend
      dockerfile: Dockerfile
      args:
        NPM_AUTH_TOKEN: ${NPM_AUTH_TOKEN}
    ports:
    - 8081:8080
  backend:
    build:
      context: ./packages/oslo-itb-rdf-validator
      dockerfile: Dockerfile
    ports:
    - 8080:8080
